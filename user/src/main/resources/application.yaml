spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/land-management
  datasource:
    url: jdbc:postgresql://localhost:5433/land_management
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

keycloak:
  server-url: http://localhost:8080
  realm: land-management
  username: admin
  password: admin
  client-id: registration-auth-flow
  client-secret: umsQgCZL1UcQLGlhCy4Ozqs3pGAZ1tAh

server:
  port: 8081
  logging:
    level:
      root: INFO
      com:
        UserService:
          user: INFO
default-role: user






